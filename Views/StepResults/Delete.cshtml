@model OMSys.Models.StepResult

@{
    ViewData["Title"] = "Delete Step Result";
}

<div class="container mt-4">
    <div class="card shadow-lg border-0 rounded-3">
        <div class="card-header bg-danger text-white text-center py-3">
            <h3 class="mb-0">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                Confirm Delete
            </h3>
        </div>
        <div class="card-body p-4">
            <h5 class="text-danger fw-bold">Are you sure you want to delete this Step Result?</h5>
            <p class="text-muted">This action cannot be undone.</p>
            <hr />

            <dl class="row">
                <dt class="col-sm-4 fw-semibold">Result Option</dt>
                <dd class="col-sm-8">@Model.ResultOption</dd>

                <dt class="col-sm-4 fw-semibold">Step Instruction</dt>
                <dd class="col-sm-8">@(Model.Step?.Instruction ?? "N/A")</dd>

                <dt class="col-sm-4 fw-semibold">Solution Details</dt>
                <dd class="col-sm-8">@(Model.Solution?.IndicationAndRepair ?? "N/A")</dd>

                <dt class="col-sm-4 fw-semibold">Next Step Instruction</dt>
                <dd class="col-sm-8">@(Model.NextStep?.Instruction ?? "N/A")</dd>
            </dl>

            <form asp-action="Delete" class="mt-4 d-flex justify-content-between">
                <input type="hidden" asp-for="ResultId" />
                <a asp-action="Index" class="btn btn-outline-secondary px-4">
                    <i class="bi bi-arrow-left-circle"></i> Cancel
                </a>
                <button type="submit" class="btn btn-danger px-4">
                    <i class="bi bi-trash-fill"></i> Delete
                </button>
            </form>
        </div>
    </div>
</div>
