@model OMSys.Models.Solution

@{
    ViewData["Title"] = "Edit Solution";
}

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-warning text-dark">
            <h3 class="mb-0">@ViewData["Title"]</h3>
        </div>
        <div class="card-body">
            <form asp-action="Edit" method="post">
                <input type="hidden" asp-for="SolutionId" />

                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="SymptomId" class="form-label"></label>
                    <select asp-for="SymptomId" class="form-select" asp-items="ViewBag.SymptomId"></select>
                    <span asp-validation-for="SymptomId" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="IndicationAndRepair" class="form-label"></label>
                    <input asp-for="IndicationAndRepair" class="form-control" placeholder="Enter solution Indication And Repair..." />
                    <span asp-validation-for="IndicationAndRepair" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Cancel
                    </a>
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-save"></i> Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
